"""Alice service-layer packages (out-of-process verifiers / workers).

Currently hosts the verification VPS (``verification_vps``): the CPU logprob
re-scoring service + the transient sampled verification hand-off (plan §7).
"""
